name: JadGens
version: ${project.version}
main: ml.jadss.jadgens.JadGens
load: STARTUP
api-version: '1.13'
authors: [Jadss_pt]
softdepend: [Vault, PlaceHolderAPI, PlayertPoints, IridiumSkyblock, SuperiorSkyblock, SuperiorSkyblock2, FabledSkyblock]
description: JadGens is a machines plugin that was created with <3
prefix: "[JadGens]> "

website: jadss.ml
commands:
  JadGens:
    usage: /<command>
    description: Main command of the plugin JadGens!